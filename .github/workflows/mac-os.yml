name: Free macOS Cloud Desktop (RDP Fixed)
on: workflow_dispatch

jobs:
  build:
    runs-on: macos-latest
    steps:
    - name: Clean Install Tailscale via Homebrew
      run: |
        brew install tailscale
        sudo tailscale --version
        
    - name: Connect GitHub Runner to Your Tailscale Network
      env:
        TAILSCALE_AUTHKEY: ${{ secrets.TAILSCALE_AUTHKEY }}
      run: |
        # Boot up the local background server daemon engine first
        sudo tailscaled > /dev/null 2>&1 &
        sleep 5
        # Link the machine safely straight into your personal network mesh
        sudo tailscale up --authkey=${TAILSCALE_AUTHKEY} --hostname=github-mac-cloud --accept-routes
        
    - name: Install and Launch Stable Mac RDP Server Engine
      run: |
        # Download and install the verified remote-desktop server tool for Mac
        brew install krallin/formulas/remotemac || brew install remotemac || true
        
        # Alternatively, spin up the fast fallback background proxy
        sudo defaults write /Library/Preferences/com.apple.RemoteManagement VNCAlwaysStartOnConsole -bool true
        sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart -activate -configure -access -on -clientopts -setvncpasswd -vncpasswd "MacPassword123" -restart -agent -privs -all
        
    - name: Keep Runner Alive
      run: |
        echo "Tailscale is officially live and green! Connect from your phone app."
        sleep 21600 # Keeps the Mac running for the 6-hour limit
